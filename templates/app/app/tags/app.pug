app
  div.app-body
    spat-nav

  spat-modal
  spat-toast

  style(scoped, type='less').
    :scope {
      display: block;
      position: absolute;
      width: 100%;
      height: 100%;
      overflow: hidden;
    }